import { makeStyles } from "@material-ui/core";

export const styles = {
    table: {
        marginTop: 20
    },
    pagination: {
        marginTop: 20,
        marginBottom: 20
    }
};

export const useStyles = makeStyles(styles);